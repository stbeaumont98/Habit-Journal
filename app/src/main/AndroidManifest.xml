<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.stbeaumont.habitjournal">
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name="org.stbeaumont.habitjournal.model.NotificationReceiver"
            android:exported="false" />
        <service android:name=".model.NotificationService"
            android:exported="false"
            android:label="Notification Service"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="org.stbeaumont.habitjournal.model.NotificationService" />
            </intent-filter>
        </service>
        <activity
            android:name=".controller.EditHabitActivity"
            android:parentActivityName=".controller.HomeActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".controller.HomeActivity" />
        </activity>
        <activity android:name=".controller.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>